:80 {
    # Serve static SvelteKit build
    root * /srv
    file_server

    # Proxy requests starting with /api/v1/ to FastAPI
    route /api/v1/* {
        reverse_proxy fastapi.local:8000
    }
}
